#! injected ytt comment
#@ load("@ytt:data", "data")
#@overlay/match by=overlay.subset({"kind":"ConfigMap"})
apiVersion: v1
kind: ConfigMap
metadata:
  name: #@ data.values.name
data:
  rendered: "(@= data.values.owner @)"
